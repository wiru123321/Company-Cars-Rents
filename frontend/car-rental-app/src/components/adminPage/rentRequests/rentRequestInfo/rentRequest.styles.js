import { makeStyles } from "@material-ui/core/styles";
export const rentRequestStyles = makeStyles((theme) => ({
  paper: {
    padding: 8,
    marginTop: "2%",
  },
  buttons: {
    marginTop: "2%",
  },
  control: {
    marginTop: "2%",
  },
}));
